# Package README for markitdown (GUI-only repo)

This README exists so editable installs (pip install -e) can find a README file when building metadata.

For full project documentation, see the repository root README.md.
